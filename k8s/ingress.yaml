apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: revest-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: revest.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: revest-service
            port:
              number: 8080
